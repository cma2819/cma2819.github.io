import { Button } from '@mui/material';

export const CmarioButton = () => {
  return (
    <Button
      href='/'
      sx={{
        textTransform: 'none'
      }}
    >cmario.net</Button>
  );
};